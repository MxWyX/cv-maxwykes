import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      light: "#7c966a",
      main: "#4f683f",
      dark: "#253d17",
      contrastText: "#fff",
    },
    secondary: {
      light: "#5b342c",
      main: "#300d00",
      dark: "#140000",
      contrastText: "#000",
    },
  },
  typography: {},
});
